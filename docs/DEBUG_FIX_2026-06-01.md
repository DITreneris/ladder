# Debug session — F&F prep (2026-06-01)

**Status:** Resolved (layout fix local; deploy pending)  
**Owner:** [debug-steward](../.cursor/agents/debug-steward.md)  
**Related:** [CHANGELOG.md](../CHANGELOG.md) `[Unreleased]`, [DEBUG_REPRO.md](DEBUG_REPRO.md), [DEBUG_ENV_TRIAGE.md](DEBUG_ENV_TRIAGE.md)

---

## TL;DR

| | |
|---|---|
| **User report** | “The ladder narrows during gameplay.” |
| **Root cause** | `body` used flex centering without a stable `#app` width. After the first tap (HUD/memo/rung reflow), the flex item `#app` shrank ~90px, pulling the entire phone shell and ladder column with it. |
| **Not the cause** | Game engine logic, reorg mechanics, `max-w-[5.5rem]` rung slots, HR memo content alone, or intentional difficulty scaling. |
| **Fix** | `index.html`: `grid place-items-center` + `#app` with `w-full max-w-md min-w-0`. `style.css`: explicit `#app` width rules + Telegram full-width override. |
| **Session cost** | ~4 hours — mostly spent on wrong hypotheses before measuring layout width at tap 0 vs tap 1. |

---

## Timeline

| Phase | Focus | Outcome |
|-------|--------|---------|
| **0 — Env triage** | Prod bundle stale vs repo | Documented in [DEBUG_ENV_TRIAGE.md](DEBUG_ENV_TRIAGE.md); redeploy required before device QA |
| **1 — Trust hotfix** (`c253d26`) | Game-over blocked on network; share API; rate limit order | Shipped |
| **2 — Reactions + layout** (`8ae68f6`) | Death emoji, coffee visual, tap throttle feedback, white game surface | Shipped |
| **3 — Debug UX** (local) | Plain-English debug panel, `#imminentHint`, throttle toast | Local; see CHANGELOG `[Unreleased]` |
| **4 — Ladder width** (this doc) | “Ladder narrows mid-run” | Root cause found; fix in `index.html` + `style.css` |

---

## Symptoms

- Ladder and tap deck appeared to **shrink horizontally** after the first few taps.
- Sometimes confused with “four bands” (HUD / play / tap width mismatch) or shorter rungs when HR memo steals vertical space.
- Reproducible in browser preview **and** simulated Telegram mode (`cl-in-telegram`).

---

## Root cause

### What we measured

Playwright on 390×844 viewport, production preview:

| Moment | `#gamePlayArea` width | `#gameContentColumn` width | `#app` width |
|--------|----------------------|------------------------------|--------------|
| Tap 0 (game start) | **283px** | **315px** | **341px** |
| Tap 1 (after first climb) | **193px** | **225px** | **251px** |

After fix (`grid` + stable `#app`):

| Taps 0–8 | `#gamePlayArea` width |
|----------|------------------------|
| All | **316px** (stable until game-over screen hides play area) |

### Mechanism

1. `index.html` used `body { flex; items-center; justify-center }` with an empty `#app` wrapper.
2. `.cl-phone-shell` inside `#app` is `w-full` — circular dependency with parent sizing.
3. On first tap, content reflows (HR memo queue, `#imminentHint`, `renderRungs`, climb animation).
4. Flex layout recomputed `#app` **min-content** width downward (~90px).
5. Everything inside — HUD, play area, ladder rails, tap deck — scaled down together.

Showing HR memo **alone** (toggle visibility without a tap) did **not** change width. The shrink required the full tap + reflow cycle inside the unstable flex root.

### Fix (files)

- `apps/mini-app/index.html` — `grid place-items-center`; `#app` class `w-full max-w-md min-w-0`
- `apps/mini-app/src/style.css` — `#app { width: 100%; max-width: 28rem; min-width: 0 }` and `html.cl-in-telegram #app { max-width: none; height: 100% }`

---

## Ruled out

| Hypothesis | Why rejected |
|------------|--------------|
| HR memo rail steals horizontal space | Memo show/hide alone keeps play width at 283px |
| Old `w-48` (192px) ladder | Removed in v1.8.2; no `w-48` in current source |
| `max-w-[5.5rem]` rung slots | Caps badge columns, not shell width; connectors still span play area |
| Reorg slide / rung advance animation | Vertical transforms only; not the ~90px shell shrink |
| Vertical rung compression | HR memo reduces play **height** and rung row height (40–52px) — real but separate from horizontal bug |
| Game logic / obstacle RNG | Width changed before promotion or rank change |

---

## Debug UX improvements (same session)

These reduce “what is the game doing?” confusion but did **not** fix the width bug:

| Change | File(s) | Purpose |
|--------|---------|---------|
| Plain-English debug panel | `lib/debug.ts` | Next rung, last tap, 5-event history |
| `localStorage cl_debug=1` | `lib/debug.ts` | Persist debug when Telegram strips `?debug=1` |
| `#imminentHint` HUD line | `app.ts`, `template.ts` | First 12 rungs + debug mode |
| Throttle toast | `app.ts` | “Too fast — one tap per beat” when UI rejects tap |

See [DEBUG_REPRO.md](DEBUG_REPRO.md) for manual R1–R5 / L1–L5 checklists.

---

## Commits and deploy

| Commit | Summary |
|--------|---------|
| `c253d26` | Trust UX (game-over IO, share, rate limit) — pushed |
| `8ae68f6` | Reaction + layout unify — pushed |
| *(working tree)* | Debug UX + `#app` width fix + this doc — **not yet pushed** |

**Before F&F:** redeploy Vercel; confirm new bundle hash in [DEBUG_ENV_TRIAGE.md](DEBUG_ENV_TRIAGE.md); device QA rows for ladder width at tap 0 vs tap 5.

---

## Lessons learned — save time next time

### 15-minute triage order

1. **Bundle hash** — prod HTML vs local build ([DEBUG_ENV_TRIAGE.md](DEBUG_ENV_TRIAGE.md)). Stale prod wastes hours.
2. **Measure width at tap 0 vs tap 1** — `#gamePlayArea.clientWidth` and `#gameContentColumn.clientWidth` (use `clientWidth`, wait ≥500ms after tap).
3. **`npm run qa:layout`** — HUD / play / tap column alignment.
4. **`npm run qa:viewport`** — overflow and rung fit.
5. **Separate buckets** — layout width vs reaction/emoji vs network/API trust. Do not debug all three as one mystery.
6. **Game logic last** — only after layout metrics are stable.

### Measurement pitfalls (this session)

| Pitfall | Effect |
|---------|--------|
| `window.engine` not exposed | Programmatic `handleTap` silently no-op’d; looked like stable width |
| `getBoundingClientRect` during animations | Noisy widths (283 → 192 → 322 oscillation) |
| Clicks during death shake | Playwright “element not stable” false failures |
| Confusing height shrink with width shrink | HR memo shortens rungs; user said “susiaureja” (narrow) — check both axes |

### Recommended guardrail (future)

Extend `apps/mini-app/scripts/layout-audit.mjs` to simulate **one tap** and assert play width delta ≤ 2px. Run in CI after `qa:layout`.

---

## Cross-links

| Document | Role |
|----------|------|
| [CHANGELOG.md](../CHANGELOG.md) `[Unreleased]` | User-facing fix bullets with link here |
| [DEBUG_REPRO.md](DEBUG_REPRO.md) | Manual repro R1–R5, L1–L5 |
| [DEBUG_ENV_TRIAGE.md](DEBUG_ENV_TRIAGE.md) | Prod bundle / deploy verification |
| [DOCS_INDEX.md](../DOCS_INDEX.md) | Task router → `debug-triage` skill |
| [.cursor/agents/debug-steward.md](../.cursor/agents/debug-steward.md) | Agent owner for debug history and triage order |
| [.cursor/skills/debug-triage/SKILL.md](../.cursor/skills/debug-triage/SKILL.md) | Step-by-step triage workflow |

---

## Changelog entries (reference)

These `[Unreleased]` items belong to this session — detail here, summary in CHANGELOG:

- **Ladder narrows mid-run after first tap** — flex `#app` shrink; grid + stable width
- **Debug mode hard to read** — multi-line panel, persisted `cl_debug`
- **Game logic unclear in first runs** — `#imminentHint`
- **Throttled taps silent** — toast on UI throttle
