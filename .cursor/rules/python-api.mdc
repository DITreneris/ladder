---
description: Python FastAPI and Telegram bot conventions
globs: "{packages/api/**,apps/bot/**}"
alwaysApply: false
---

# Python API & Bot

## API Layout (`packages/api/app/`)

- `main.py` — FastAPI app, CORS, router mounts
- `auth/telegram.py` — initData HMAC validation
- `routes/` — auth, runs, leaderboard (thin handlers)
- `routes/_users.py` — user upsert helper (shared by auth + runs)
- `db/supabase.py` — Supabase client singleton
- `models.py` — Pydantic request/response models

## Conventions

- Validate Telegram `initData` on every authenticated endpoint.
- Score sanity checks: years 0–100, rungs ≈ years × 4, rate limit 10s per user.
- `GET /leaderboard` accepts optional `initData` query param to mark `is_current_user`.
- Rate limit is in-memory per process — breaks with multiple API replicas (see architecture.md).
- All DB writes use service role key; never expose to client.
- Use pydantic models for request/response; no raw dicts in route signatures.
- Bot (`apps/bot/`) only handles `/start` and Mini App button — keep minimal.

## Testing

```bash
cd packages/api && pip install -r requirements.txt && pytest
```

## Deploy

Railway Dockerfile in `packages/api/`. Health check: `GET /health`.
